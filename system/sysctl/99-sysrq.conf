# Arch ships kernel.sysrq = 16 in /usr/lib/sysctl.d/50-default.conf, which
# disables REISUB. Enable all sysrq ops so Alt+SysRq+REISUB works as a last
# resort when the compositor or GPU driver is wedged.
kernel.sysrq = 1
