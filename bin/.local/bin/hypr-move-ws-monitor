#!/usr/bin/env bash
# Move the active workspace to an adjacent monitor.
# Usage: hypr-move-ws-monitor +1   (right)
#        hypr-move-ws-monitor -1   (left)
ws_id=$(hyprctl activeworkspace -j | jq .id)
hyprctl dispatch moveworkspacetomonitor "$ws_id" "$1"
