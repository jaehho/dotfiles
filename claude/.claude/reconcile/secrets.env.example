# Secrets used by claude-reconcile to substitute ${VAR} placeholders
# in mcp-servers.json. Copy to ~/.config/claude-mcp-secrets.env
# (or set $CLAUDE_MCP_SECRETS_FILE) and fill in real values. Do NOT commit.

ZOTERO_API_KEY=
