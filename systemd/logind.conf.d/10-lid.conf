# Lid close suspends to RAM. Rollback to lock if suspend regressions surface
# (proprietary nvidia 595.x required; nvidia-open breaks all sleep-resume).
[Login]
HandleLidSwitch=suspend
HandleLidSwitchExternalPower=suspend
