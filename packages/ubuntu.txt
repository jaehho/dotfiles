# Packages needed for common stow configs to work on Ubuntu/Debian.
# Install with: make pkg-install (or `make sync` for full setup).
#
# Tools not in apt — `make sync` handles these:
#   eza        — https://github.com/eza-community/eza (in apt on Ubuntu 24.10+)
#   zoxide     — https://github.com/ajeetdsouza/zoxide
#   bat        — available as 'batcat'; `make sync` symlinks ~/.local/bin/bat
#   fd         — available as 'fdfind'; `make sync` symlinks ~/.local/bin/fd

# Shell
fish

# Core tools
git
curl
stow
tmux
neovim

# File tools
fzf
fd-find
ripgrep
bat

# Dev
direnv
jq
gcc
make
nodejs
npm
python3
python3-pip
luarocks

# Keyd build deps (no apt package — built from source by make system-install)
libudev-dev

# Misc
rclone
sshpass
libnotify-bin
ffmpeg
openssh-client
unzip
