# Ubuntu/Debian-only packages; common.txt holds the names shared with Arch.
# uv and Claude Code come from their own installers (converge's tools step).

# Core tools
curl
gettext-base

# File tools
fd-find

# Dev
gcc
git-email
make
python3
python3-pip
rustup

# Misc
libudev-dev
libnotify-bin
gstreamer1.0-libav
openssh-client

# Upgrades: converge installs but never upgrades on Ubuntu; this does
unattended-upgrades
