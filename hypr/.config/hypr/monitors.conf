# Managed by hypr-monitor — regenerated on every hotplug event (see the
# `hypr-monitor watch` autostart in hyprland.conf).
#
# The fallback rule below keeps unlisted monitors auto-configured if the daemon
# hasn't written its output yet (e.g. on first boot before autostart completes).

monitor=,preferred,auto,auto
